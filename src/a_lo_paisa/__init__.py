"""Paquete a_lo_paisa: pipeline de voz STT → agente paisa → TTS.

Este archivo está casi vacío a propósito. Su sola presencia convierte la
carpeta `a_lo_paisa/` en un paquete de Python importable (p. ej.
`from a_lo_paisa import config`). Aquí solo exponemos la versión.
"""

__version__ = "0.1.0"
